import React from 'react';
import styles from './MoonRace.module.css';

const Header = () => (
  <header className={styles.titleRow}>
    <h1 className={styles.mainTitle}>
      Space Exploration's Geopolitical Narratives
    </h1>
  </header>
);

export default Header;
